package java;

import java.util.Base64;

public class TestingBase64 {
//  public static void main(String[] args) {
//    String str = "cybozu" + ":" + "aaaaaaaaaabbbbbbbbbbccccccccccddddddddddeeeeeeeeeef";
//    String result = Base64.encodeToString(str.getBytes(), Base64.DEFAULT).trim();
//    for (int i = 0, length = result.length(); i < length; i++) {
//      char c = result.charAt(i);
//      if ((c <= '\u001f' && c != '\t') || c >= '\u007f') {
//        throw new IllegalArgumentException(
//            String.format("Unexpected char %#04x at %d in %s value: %s", (int) c, i, "toto",
//                result));
//      }
//    }
//  }
}
